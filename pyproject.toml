[project]
name = "fastapi-app"
version = "0.1.0"
description = "A FastAPI application"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]==0.115.12",
    "openai==1.65.4"
]

[dependency-groups]
dev = [
    "ruff>=0.11.2",
]
