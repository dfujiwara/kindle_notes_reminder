from src.repositories.book_repository import BookRepository
from src.repositories.note_repository import NoteRepository
from src.notebook_parser import NotebookParseResult


def process_notebook_result(result: NotebookParseResult, book_repo: BookRepository, note_repo: NoteRepository):
    # Example logic to process the result
    # This is where you would interact with the repositories
    # For example, you might want to add books or notes to the database
    pass
